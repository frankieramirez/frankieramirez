---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div
  class:list={[
    "grid grid-cols-1 md:grid-cols-4 gap-4 auto-rows-[minmax(180px,auto)] max-w-7xl mx-auto p-4",
    className,
  ]}
>
  <slot />
</div>
